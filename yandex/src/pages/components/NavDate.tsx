


/**
 * A simple component that displays a navigation date.
 *
 * @returns {JSX.Element} The JSX element representing the component.
 */
export default function NavDate({children}: React.PropsWithChildren): JSX.Element {
  
 

    return (
      
        <div>
            {children}
        </div>
          
      
    );
  }